/**
 * ValidateRoles
 *
 * ValidatePermissions
 *
 * JWT -> Enviar el grupo del usuario
 *
 * req.group as string
 *
 * Guardar en memoria los permisos en un servicio
 *
 * obtener los permisos del grupo
 *
 * groupPermissions.includes(permission)
 *
 * @PermissionsGuard('Permission')
 * @PermissionsGuard('Permission', 'Permission')
 * !TODO URGENTE:
 * Al enviar las cookies mal formadas el backend se bloquea
 */
