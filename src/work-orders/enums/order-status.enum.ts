export enum OrderStatus {
  pending_approval = 'pending_approval',
  approved = 'approved',
  rejected = 'rejected',
  scheduled = 'scheduled',
  in_progress = 'in_progress',
  completed = 'completed',
}
